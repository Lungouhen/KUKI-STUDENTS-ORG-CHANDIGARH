{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build"
    },
    "comments": {
        "vendor": "Front-end libraries (Bootstrap, Alpine, SweetAlert2, ApexCharts, FullCalendar, CKEditor, FontAwesome) are served as self-hosted files from public/vendor/ and are NOT bundled by Vite. They are intentionally absent from dependencies; only modules actually imported by resources/js belong here."
    },
    "devDependencies": {
        "axios": "^1.7.4",
        "laravel-vite-plugin": "^1.0.0",
        "vite": "^5.0.0"
    }
}
